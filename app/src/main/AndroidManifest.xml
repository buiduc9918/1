<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/chat_conversation_svgrepo_com"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chat_conversation_svgrepo_com"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".Activity.ChatActivity2"
            android:exported="false" />
        <activity
            android:name=".Activity.SearchUserActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.MainActivity2"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".Activity.AuthActivity"
            android:exported="true"
            android:label="@string/title_activity_auth"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>